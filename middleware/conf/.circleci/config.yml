version: 2
jobs:
  build:
    docker:
      - image: circleci/node:8.1.2
    steps:
      - checkout
      - run: npm i
  test:
    docker:
      - image: circleci/node:8.1.2
    steps:
      - checkout
      - run: npm test
workflows:
  version: 2
  build_and_test:
    jobs:
      - build
      - test